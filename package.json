{
  "name": "@agent-teams/extension-foundation-repository",
  "version": "0.0.0",
  "private": true,
  "description": "Product-neutral extension artifact, trust, distribution, isolation, and conformance primitives for Agent Teams products.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=24.18.0 <25",
    "pnpm": ">=11.17.0 <12"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agent-teams-ai/extension-foundation.git"
  },
  "scripts": {
    "architecture:check": "pnpm architecture:topology:check && pnpm architecture:source-dependencies:check && node --import=\"data:text/javascript,process.env.PACKAGE_POLICY_TEST_MODE='full'\" --test --test-concurrency=1 tests/architecture-topology.test.mjs tests/evidence-custody.test.mjs tests/package-policy-characterization.test.mjs tests/package-policy-source-boundaries.test.mjs tests/scaffolding.test.mjs",
    "architecture:check:fast": "pnpm architecture:topology:check && pnpm architecture:source-dependencies:check && node --import=\"data:text/javascript,process.env.PACKAGE_POLICY_TEST_MODE='fast'\" --test --test-concurrency=1 tests/architecture-topology.test.mjs tests/evidence-custody.test.mjs tests/package-policy-characterization.test.mjs tests/package-policy-source-boundaries.test.mjs tests/scaffolding.test.mjs",
    "architecture:package-artifacts:check": "node architecture/checks/package-artifacts.mjs",
    "architecture:scaffold:apply": "node architecture/checks/scaffold.mjs apply",
    "architecture:scaffold:plan": "node architecture/checks/scaffold.mjs plan",
    "architecture:scaffold:recover": "node architecture/checks/scaffold.mjs recover",
    "architecture:source-dependencies:check": "agent-teams-foundation check architecture.source-dependencies",
    "architecture:topology:check": "node architecture/checks/package-topology.mjs",
    "evidence:custody": "node architecture/checks/evidence-custody-cli.mjs",
    "evidence:custody:test": "node --test --test-concurrency=1 tests/evidence-custody.test.mjs",
    "check": "pnpm foundation:check && pnpm docs:protocol:check && pnpm architecture:check && pnpm typecheck && pnpm qualification:check && pnpm packages:check",
    "check:changed": "agent-teams-foundation agent-workflow changed --consumer .",
    "check:fast": "pnpm foundation:check && pnpm docs:protocol:check && pnpm architecture:check:fast && pnpm typecheck && pnpm packages:check",
    "docs:changed": "pnpm docs:check && pnpm docs:quality",
    "docs:check": "agent-teams-docs check --consumer . --profile architecture/foundation/docs-protocol.yaml",
    "docs:contract-test": "node --test tests/document-authoring.test.mjs tests/docs-protocol-qualification.test.mjs",
    "docs:doctor": "agent-teams-docs doctor --consumer . --profile architecture/foundation/docs-protocol.yaml",
    "docs:find": "agent-teams-docs find --consumer . --profile architecture/foundation/docs-protocol.yaml",
    "docs:info": "agent-teams-docs info --consumer . --profile architecture/foundation/docs-protocol.yaml",
    "docs:new": "agent-teams-docs new --consumer . --profile architecture/foundation/docs-protocol.yaml",
    "docs:protocol:check": "pnpm docs:check && pnpm docs:quality && pnpm docs:contract-test",
    "docs:quality": "markdownlint-cli2 && cspell --config .cspell.json --no-progress",
    "docs:recover": "agent-teams-docs recover --consumer . --profile architecture/foundation/docs-protocol.yaml",
    "foundation:assert-dev-only": "agent-teams-foundation assert-dev-only",
    "foundation:assert-registry": "agent-teams-foundation assert-registry",
    "foundation:attach": "agent-teams-foundation attach",
    "foundation:check": "agent-teams-foundation check && pnpm foundation:assert-dev-only && pnpm foundation:assert-registry",
    "foundation:detach": "agent-teams-foundation detach",
    "foundation:status": "agent-teams-foundation status",
    "packages:artifacts:check": "pnpm architecture:package-artifacts:check",
    "packages:check": "pnpm --recursive --filter \"./packages/**\" check && pnpm packages:artifacts:check",
    "qualification:check": "pnpm qualification:typecheck && pnpm qualification:dogfooding-model:check && node --test --test-concurrency=1 tests/qualification/*.test.ts",
    "qualification:browser-worker:check": "node --test --test-concurrency=1 --test-name-pattern=\"browser Worker carries a portable generation-bound frame\" tests/qualification/spikes.test.ts",
    "qualification:dogfooding-model:check": "node architecture/checks/dogfooding-protocol-model.mjs",
    "qualification:packed-consumer:check": "node --test --test-concurrency=1 --test-name-pattern=\"packed toy package exercises an isolated consumer\" tests/qualification/spikes.test.ts",
    "qualification:product-sources:check": "node architecture/checks/product-source-evidence-cli.mjs docs/qualification/module-system-v1-productization/consumer-source-evidence.yaml",
    "qualification:extism:experiment": "node tests/qualification/extism-feasibility.mjs",
    "qualification:typecheck": "tsc --project tsconfig.qualification.json --noEmit --pretty false",
    "typecheck": "tsc --project tsconfig.json --noEmit --pretty false && pnpm --recursive --filter \"./packages/**\" typecheck"
  },
  "devDependencies": {
    "@agent-teams/docs-protocol": "0.2.0",
    "@agent-teams/engineering-foundation": "0.20.0",
    "@dagrejs/graphlib": "catalog:",
    "@deepseek-ai/cordis": "catalog:",
    "@extism/extism": "catalog:",
    "@types/node": "catalog:",
    "cspell": "catalog:",
    "fast-check": "catalog:",
    "markdownlint-cli2": "catalog:",
    "oxc-parser": "catalog:",
    "typescript": "catalog:",
    "yaml": "catalog:"
  }
}
