name: Documentation Protocol

on:
  pull_request:
  merge_group:
  push:

permissions:
  contents: read
  id-token: write

jobs:
  docs-protocol:
    uses: agent-teams-ai/.github/.github/workflows/docs-protocol-check.yml@94190f4df44b536804fdef8341eab226d2b94f9c
